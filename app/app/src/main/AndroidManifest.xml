<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.springboks.takeawaymessenger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.CreateCustomerActivity"
            android:label="@string/title_activity_create_customer"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".activities.SplashActivity" />
        <activity android:name=".activities.LoginOrderActivity" />
        <activity android:name=".activities.CreateCourierActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity
            android:name=".activities.OrderActivity"
            android:label="@string/order_label" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/orders_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>